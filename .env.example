# Docker Configuration
# NOTE: DOCKER_BASE_URL is auto-detected based on your OS (Windows/Linux/macOS/WSL).
# Only set this if you need to override the default or connect to a remote Docker host.
# - Linux/macOS/WSL (default): unix:///var/run/docker.sock
# - Windows (default):         npipe:////./pipe/docker_engine
# - Remote TCP:                tcp://host:port
# DOCKER_BASE_URL=unix:///var/run/docker.sock
DOCKER_TIMEOUT=60
DOCKER_TLS_VERIFY=false
# DOCKER_TLS_CA_CERT=/path/to/ca.pem
# DOCKER_TLS_CLIENT_CERT=/path/to/cert.pem
# DOCKER_TLS_CLIENT_KEY=/path/to/key.pem

# Safety Configuration
# SAFETY_ALLOW_MODERATE_OPERATIONS: Allow state-changing operations (create, start, stop, pull, etc.)
#   - false = Read-only mode (safest, only list/inspect operations)
#   - true  = Default mode (allow create, start, stop, etc.)
SAFETY_ALLOW_MODERATE_OPERATIONS=true
SAFETY_ALLOW_DESTRUCTIVE_OPERATIONS=false
SAFETY_ALLOW_PRIVILEGED_CONTAINERS=false
SAFETY_REQUIRE_CONFIRMATION_FOR_DESTRUCTIVE=true
SAFETY_MAX_CONCURRENT_OPERATIONS=10

# Security Configuration
# WARNING: Set SECURITY_AUTH_ENABLED=true for production use!
# Authentication is SSH key-based only (API key auth removed for security)
SECURITY_AUTH_ENABLED=false

# SSH Authentication (enabled when SECURITY_AUTH_ENABLED=true)
# SECURITY_SSH_AUTH_ENABLED=true
# SECURITY_SSH_AUTHORIZED_KEYS_FILE=~/.mcp-docker/authorized_keys

# Rate Limiting
SECURITY_RATE_LIMIT_ENABLED=true
SECURITY_RATE_LIMIT_RPM=60
SECURITY_RATE_LIMIT_CONCURRENT=3

# Audit Logging
SECURITY_AUDIT_LOG_ENABLED=true
SECURITY_AUDIT_LOG_FILE=mcp_audit.log

# Network Security (Optional)
# Leave empty to allow all IPs, or specify allowed IPs:
# SECURITY_ALLOWED_CLIENT_IPS=["127.0.0.1", "192.168.1.100"]

# Server Configuration
MCP_SERVER_NAME=mcp-docker
MCP_SERVER_VERSION=0.1.0
MCP_LOG_LEVEL=INFO
# MCP_DOCKER_LOG_PATH=/path/to/mcp_docker.log  # Custom log file path (optional)
